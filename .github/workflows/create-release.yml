name: create-release
on:
  push:
    branches:
      - main
jobs:
  generate_manpages:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: ./setup.bash --generate-release
        with:
          repo_token: "${{ secrets.GITHUB_TOKEN }}"